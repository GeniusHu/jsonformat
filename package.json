{
  "name": "json-tools",
  "version": "1.0.0",
  "description": "Online JSON tools including formatter, validator, editor and more",
  "main": "index.js",
  "scripts": {
    "prepare": "node scripts/prepare.js",
    "dev": "npm run prepare && vite",
    "build": "npm run prepare && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "codemirror": "^5.65.18",
    "jsonlint-mod": "^1.7.6"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "vite": "^4.3.9"
  }
}
